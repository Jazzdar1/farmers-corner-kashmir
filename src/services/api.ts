// src/services/api.ts

export async function callGemini(prompt: string) {
  // Temporary mock response
  return {
    weather: "Sunny",
    temperature: "22°C",
    advice: "Good day for irrigation and pest inspection"
  };
}
